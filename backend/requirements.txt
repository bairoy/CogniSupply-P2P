fastapi
uvicorn[standard]
pydantic
psycopg2-binary
redis
python-multipart

# Auth. HS256 JWTs signed with JWT_SECRET (shared/auth.py) -- every service
# verifies locally, so login does not become a network hop on every request.
# bcrypt for password hashing: never store or compare a plaintext password.
PyJWT
bcrypt

# AI layer (NLP requisition parsing, vision OCR, supplier reasoning).
# Optional at runtime: every AI path falls back to a deterministic stub when
# ANTHROPIC_API_KEY is unset, so the system still runs end to end without it.
anthropic

# Invoice image rendering for the OCR path.
pillow

# Dock scheduling. CP-SAT solves the trailer -> door -> time-window assignment
# for the whole pending set (shared/dock_engine.py). Optional at runtime: if the
# import fails the engine falls back to a deterministic first-fit pass over the
# same cost model, so the yard still gets a schedule -- it just stops being an
# optimal one, and Plan.status says so.
ortools

# Simulator + seed data
faker
httpx

# Config
python-dotenv

# Tests
pytest
pytest-asyncio
websockets
